# DeepSeek Harness Desktop 项目说明书

本文件是工作区（项目根目录）的完整导航：每个文件夹、每个重要文件是干什么的，
以及日常怎么操作。适合自己备忘，也适合发给协作者。

> 路径约定（脱敏）：本文不出现任何真实盘符与用户名。
> - 工作区 = 项目根目录（源码所在）
> - 安装目录 = 客户端安装位置（默认形如 <盘符>\DeepSeek-Harness）
> - 用户目录 = 当前 Windows 用户目录（形如 <盘符>\Users\<用户名>）

---

## 一、这个目录是什么

工作区是【DeepSeek Harness Desktop】的项目根目录：

- 源码在这里（Electron 桌面端主进程 + 打包脚本 + 图标资源）
- 开发、打包、发版都在这里进行
- 同时是一个 git 仓库（远程：github.com/Misaki0427/deepseek-harness-desktop）
- 安装包产物输出到 dist 子目录，用户实际安装的位置是安装目录（与本目录无关）

---

## 二、根目录文件清单（每个文件干什么）

| 文件 | 作用 | 是否进安装包 |
|---|---|---|
| main.js | ★核心：Electron 主进程。窗口/托盘/服务启停/看门狗/崩溃自恢复/单实例/皮肤部署/自动更新/快捷键全在这里 | 是 |
| build.js | ★打包编排脚本：精简运行时 → 给 NSIS 模板打补丁 → 调 electron-builder → 注入资源 → 自动清理旧产物 | 否（仅打包时用） |
| preload.js | 渲染进程安全桥接：页面用 deepseekHarness（版本/名称），市场窗口用 marketBridge（插件市场 IPC） | 是 |
| market.js | ★插件市场模块（主进程）：市场数据多源拉取、内置 pnpm 执行装/卸/更新、bundle 启停、备份回滚、独立市场窗口 | 是 |
| market.html | 插件市场窗口 UI（鲸鱼娘主题，浏览/搜索/安装/卸载/启停/更新） | 是 |
| test-market-logic.js | 插件市场核心逻辑自动化测试（隔离临时环境运行，不碰真实配置） | 否（测试用） |
| 插件市场-可行性分析.md | 插件市场功能需求确认与可行性分析文档 | 否（git 用） |
| index.html | 主题启动页（鲸鱼娘背景 + 徽章 + 加载动画），服务就绪前显示 | 是 |
| package.json | 项目配置：版本号、npm 脚本（start/build/release:*）、electron-builder 打包配置（唯一来源）、GitHub 发布源 | 是（版本字段） |
| package-lock.json | 依赖版本锁定，保证可重复构建 | 否（构建用） |
| .gitignore | git 排除规则：node_modules、dist、harness-runtime 运行时、github-package、备份等 | 是（git 用） |
| README.md | GitHub 项目主页文档（简介/特性/架构/下载/打包/皮肤来源标注；版本历史只保留最近 3 个版本） | 否（git 用） |
| LICENSE | MIT 开源许可 | 否（git 用） |
| AGENTS.md | 全局行为指令（思考与回复统一使用中文） | 否（git 用） |
| icon.ico | 应用图标（多尺寸 256/128/64/48/32/16，来自 04 俏皮头像版，圆角化） | 是 |
| installer-icon.ico | 安装器/卸载器图标（来自 01 伸手可爱版，圆角化） | 否（打包时用） |
| tray.png | 托盘图标 32×32（来自 01 伸手眨眼版，圆角化） | 是 |
| theme-bg.png | 启动页背景图（03 鲸鱼抱枕版模糊+暗化） | 是 |
| theme-emblem.png | 启动页中央圆角徽章（03 鲸鱼抱枕版） | 是 |
| 项目说明书.md | 本文件 | 否（git 用） |

---

## 三、根目录文件夹清单（每个文件夹干什么）

| 文件夹 | 作用 | 能不能删 |
|---|---|---|
| .git | git 版本库数据（提交历史） | ❌ 删了就没版本管理了 |
| dist | ★构建产物归档：dist\版本号\Setup.exe。只保留最近两个版本 | 按需删旧版本 |
| harness-runtime | ★内置运行时：dsh-service.exe + @deepseek-ai/dsh + watchdog.js + 内置鲸鱼娘皮肤 + 内置 pnpm（插件市场安装功能）。开发模式(npm start)和打包都从这里取 | ❌ 核心，勿删 |
| market | 市场数据源 market\plugins.json（安装包内置兜底 + GitHub 在线更新） | ❌ 勿删（进安装包） |
| icon-design | 图标工程：源图、生成脚本（make-*.ps1）、旧图标备份、预览、outputs | ✅ 可整体删除（不影响运行，但改图标会用到） |
| github-package | 本地工具：双击 make-package.cmd 生成与 GitHub 一致的源码包和文件清单（本身不上传） | ✅ 可删（可重建） |
| node_modules | 开发依赖（electron、electron-builder、electron-updater），仅打包/开发用 | ✅ 可删后 npm install 重建 |
| .dsh-profile-backup | dsh 用户配置备份（装皮肤前拍的快照，用于回滚） | ✅ 可删（回滚备用） |
| tools | 辅助脚本（如本地 OCR 工具） | ✅ 可删（不影响产品） |

---

## 四、核心 JS 文件详解

### 4.1 main.js（Electron 主进程，最重要）

按区块顺序：

1. 基本配置：本地服务地址 127.0.0.1:3080；开发环境用 harness-runtime，打包环境用安装目录的 resources\harness；内置 dsh-service.exe 和 dsh 入口路径
2. checkHarness()：快速探测 3080 是否响应（状态码判断）
3. identifyService()：识别 3080 上到底是不是真 Harness（查页面里的 __DSH_BOOT__ 标记），防止误挂别的程序
4. validateHarnessRuntime()：启动前检查 dsh-service.exe / dsh / watchdog.js 是否齐全
5. killProcessTree()：用 taskkill /T /F 结束整个进程树
6. startHarness()：防重入启动流程——识别端口身份 → 拉起 watchdog.js（看门狗→dsh）→ 等 60 秒 → 二次身份确认。启动期间崩溃快速失败
7. stopHarness() / restartHarness()：停止/重启（只杀自己启动的服务，不误杀别人的）
8. 崩溃自动恢复：scheduleAutoRestart() 按 2s/4s/6s 退避重试 3 次，失败弹窗；成功后自动把窗口切回 UI
9. ensureSkinProfile()：★开盒即用皮肤——把内置鲸鱼娘皮肤部署进用户 dsh 配置并注册（不需要 pnpm），带落盘校验与自愈
10. 自动更新：electron-updater + GitHub Releases，启动 10 秒后静默检查，托盘可手动检查更新
11. openHarness() / updateTray() / createTray()：窗口唤起与托盘菜单（打开/显示/启动/停止/重启/插件市场/检查更新/打开日志/状态/退出）
12. createWindow()：创建窗口，先显示本地启动页(theme 背景)，服务就绪后切到 3080 的 Harness UI；固定窗口标题 DeepSeek Harness Desktop v版本号；导航/外链白名单；Alt+Enter 换行拦截
13. shutdown() / bootstrap()：退出清理与启动编排（托盘→窗口→皮肤→服务→UI→插件市场模块初始化→更新检查）
14. 单实例锁：重复启动只弹窗提示并聚焦已有窗口；before-quit / window-all-closed 生命周期处理

### 4.2 build.js（打包编排）

1. prepareHarnessRuntime()：把 harness-runtime 复制成临时 harness-build，剔除 .map/.md/.ts/.mts，校验关键文件（dsh-service.exe/dsh/watchdog/皮肤）
2. patchNsisTemplate()：★构建前自动给 electron-builder 的 NSIS 模板打补丁（安装目录自动创建），幂等
3. build()：调 electron-builder，打包配置全部来自 package.json（唯一来源，避免双份配置冲突）
4. afterPack 钩子：把精简运行时注入 win-unpacked 的 resources\harness
5. cleanupOldUnpacked()：★构建成功后自动删除其他版本的 win-unpacked，只保留最新版
6. finally：无论成败都删除临时 harness-build

### 4.3 preload.js

contextBridge 安全桥接：页面用 deepseekHarness（version/name），市场窗口额外用 marketBridge
（getState/install/uninstall/update/setEnabled/openHomepage/onProgress，全部走白名单 IPC，不开任何 Node 能力）。

### 4.4 harness-runtime\watchdog.js（看门狗）

由主进程用内置 dsh-service.exe 启动，它再拉起 dsh。每 3 秒检查 Electron 主进程是否存活，
一旦发现主进程被强杀，立即 taskkill 整个 Harness 进程树，杜绝残留进程占用 3080。

### 4.5 market.js（插件市场模块，主进程）

1. 市场数据：market\plugins.json 内置兜底；在线多源并行拉取（jsDelivr CDN → GitHub raw，5 秒超时），成功后缓存到 userData\market-cache.json（离线可用）
2. 已装状态：读用户 profile 的 package.json（dependencies + dsh.profile.bundles）+ node_modules 各包 manifest（pnpm symlink 安全解析）
3. 安装/卸载/更新：内置 dsh-service.exe 执行内置 pnpm.cjs（用户机器无需装 node/pnpm），完成后照官方 dsh plugin 逻辑自动 reconcile bundles
4. 启用/禁用：直接增删 dsh.profile.bundles（无 BOM UTF-8 写回）；内置 bundle（dsh-base/web-app/皮肤）拒绝操作
5. 安全：包名白名单正则防注入、市场 JSON schema 校验、操作前备份 profile 到 .dsh-profile-backup、失败自动回滚、操作串行化（同一时刻只跑一个 pnpm）
6. dsh-img 兼容补丁：安装/更新后自动补 exports 字段 + client.js shim（保持图片识别插件可用）
7. 窗口：托盘「插件市场」打开独立窗口（单例，950×680，鲸鱼娘主题，CSP 限制 + XSS 转义）

---

## 五、图标资源关系（icon-design 工程）

| 源图 | 去向 | 用途 |
|---|---|---|
| 源图\04_俏皮头像版.png | icon.ico | 应用图标（桌面/开始菜单/任务栏） |
| 源图\01_伸手可爱版.png | installer-icon.ico | 安装器图标 |
| 源图\01_伸手眨眼版.png | tray.png | 托盘图标 |
| 源图\03_鲸鱼抱枕版.png | theme-bg.png + theme-emblem.png | 启动页主题背景+徽章 |

生成脚本：make-icons.ps1（四件套）、make-tray.ps1（矢量鲸鱼娘托盘）、make-tray2.ps1（插画托盘）。
圆角参数 0.22（越大越圆）。旧图标备份和矢量托盘在 旧图标备份\ 目录。

---

## 六、常用操作速查

| 想干什么 | 命令 |
|---|---|
| 开发模式启动 | npm start |
| 打包（版本不变） | 对 AI 说「请打包」→ 由 AI 执行 npm run build |
| 普通改动发版 | 确认后由 AI 执行 npm run release:patch（2.x.y → 2.x.y+1） |
| 较大变更发版 | npm run release:minor（2.x.y → 2.x+1.0） |
| 特大变动发版 | npm run release:major（→ 3.0.0） |
| 推到 GitHub | git push（需开代理 TUN 模式） |
| 上传安装包 | GitHub Releases 页面拖入 dist\版本号\Setup.exe + latest.yml |
| 查看 GitHub 上传清单 | 双击 github-package\make-package.cmd |
| 卸载皮肤插件 | dsh plugin --profile web remove @dsh-external/dsh-client-ui-skin-maid-atelier |
| 内置 pnpm（市场安装功能依赖，一次性） | cd harness-runtime 后执行 pnpm add pnpm（开发机与打包前各需一次） |
| 插件市场操作 | 托盘右键 →「插件市场」（装/卸/启停/更新插件，无需命令行） |
| 回滚 dsh 配置 | 把 .dsh-profile-backup\web\ 4 个文件复制回 用户目录\.dsh\profiles\web\ |

⚠️ 打包流程（重要）：AI 修改代码后【不自动打包】；你审阅、测试（npm start）确认无误后，
对 AI 说「请打包」，AI 才执行构建。发版命令 = 升版本号 + 打包 + 输出到 dist\新版本\（互不覆盖）。

README 维护规则：版本历史表只保留最近 3 个版本，更早的归档到 GitHub Releases。

---

## 七、数据都存放在哪里（重装/卸载不受影响）

| 数据 | 位置 | 说明 |
|---|---|---|
| API Key | 用户目录\.dsh\.credentials.yaml | 用户级，不在安装包里，分享 exe 不会泄露 |
| 会话记录 | 用户目录\.dsh\sessions | 同上 |
| dsh 插件/皮肤配置 | 用户目录\.dsh\profiles\web | 皮肤自动部署也写这里 |
| 浏览器缓存 | %APPDATA%\deepseek-harness-desktop | Chromium 数据 |
| 安装目录 | 安装目录（形如 <盘符>\DeepSeek-Harness） | 覆盖安装即可升级 |

卸载/重装只会动安装目录，上面所有用户数据都保留。

---

## 八、版本历史速览

- 2.2.1：修复主动停止/重启时 Manager 退出事件晚到被误判「非预期退出」而触发自动恢复的竞态（stoppedChild 标记）
- 2.2.0：插件市场（托盘独立窗口，浏览/搜索/一键安装/卸载/启停/更新，内置 pnpm 零环境依赖）
   + 托盘检查 Harness 更新（内置 npm 一键拉取新版本、dump-config 校验、失败自动回滚）
   + 精简托盘菜单（四块结构：窗口与状态 / 服务操作 / 市场与更新 / 退出）
- 2.1.5：托盘重启气泡提示 + 退出竞态修复 + 日志按本地日期分文件
- 2.1.4：聊天框 Alt+Enter 换行 + 中文行为指令 + 本地 OCR 工具
- 2.1.3：修复皮肤校验函数 return 换行 ASI 陷阱（皮肤自动恢复）
- 2.1.2：皮肤部署非破坏式覆盖 + 逐步诊断日志
- 2.1.1：修复皮肤部署路径双作用域 bug
- 2.1.0：自动更新 + 托盘检查更新 + 安装目录自动创建
- 2.0.13：内置运行时改名 dsh-service.exe（规避安全软件对 node.exe 的特征拦截）
- 2.0.12：皮肤部署双保险 + 落盘校验 + 启动自愈
- 2.0.11：修复皮肤自动部署缺失文件导致新机器启动崩溃
- 2.0.10：失败弹窗新增「复制日志」按钮
- 2.0.9：黑匣子文件日志
- 2.0.8：内置鲸鱼娘皮肤，开盒即用；窗口标题带版本号
- 2.0.7：窗口标题固定 + 皮肤来源标注
- 2.0.6 及更早：托盘图标/鲸鱼娘四件套/稳定性安全大版本等，详见 GitHub Releases 归档

## 九、故障排查（黑匣子日志）

应用自带文件日志：%APPDATA%\deepseek-harness-desktop\logs\desktop-YYYY-MM-DD.log（按本地日期每天一个文件，跨天自动切换；单日超过 2MB 自动轮转为 .old）。
启动失败/自动恢复失败的弹窗会直接显示最近日志；托盘菜单有「打开日志文件夹」。

两种典型报错含义：

1. "无法连接 Harness：http://127.0.0.1:3080" —— 服务 60 秒内没起来（启动阶段失败）。
   常见原因：dsh 启动崩溃（看日志堆栈）、3080 被占（占用方响应异常时也会走到这里）。
2. "Harness 服务多次启动失败……自动恢复 3 次后" —— 服务曾启动成功，之后被反复杀死。
   常见原因：安全软件杀掉未签名的运行时进程（实测案例：联想电脑管家会静默删除皮肤文件并
   终止服务进程，关闭后应用立即正常）。解决办法：把安装目录加入安全软件信任区/白名单。

排查流程：让用户发弹窗截图 + 当天日期的 desktop-YYYY-MM-DD.log 文件。

3. 报错出现 "SyntaxError: Unexpected token '锘?" —— dsh 配置 JSON 被写入了 BOM 头。
   手动编辑 用户目录\.dsh 下的 package.json 等文件时，务必保存为【无 BOM 的 UTF-8】；
   稳妥做法是用 dsh plugin 命令管理插件，不要手动改 JSON。

## 十、路线图

已闭环：稳定性加固 / 鲸鱼娘主题 / 内置皮肤 / 自动更新 / 安装目录自动创建 / 黑匣子日志 / 聊天框 Alt+Enter 换行 / 日志按天分文件 / 插件市场（2.2.0）。

待办：

1. 代码签名：购买 OV/EV 证书（约 300~1500 元/年）后接入，消除 SmartScreen 警告；免费方案不存在。
2. 图片识别收尾：dsh-img 当前未安装在用户 profile（可在插件市场一键装回）；ZHIPU_API_KEY 尚未配置（用户环境变量）。
3. 插件市场发布侧：GUI 提交插件到市场仓库（当前发布仍走 GitHub PR / 命令行）。

---

最后更新：与 README.md 同步维护；改完代码记得顺手更新本文件。
