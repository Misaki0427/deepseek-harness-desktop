# DeepSeek Harness Desktop 项目说明书

本文件是 F:\DeepSeek Harness 目录的完整导航：每个文件夹、每个重要文件是干什么的，
以及日常怎么操作。适合自己备忘，也适合发给协作者。

---

## 一、这个目录是什么

F:\DeepSeek Harness 是【DeepSeek Harness Desktop】的项目根目录：

- 源码在这里（Electron 桌面端主进程 + 打包脚本 + 图标资源）
- 开发、打包、发版都在这里进行
- 同时是一个 git 仓库（远程：github.com/Misaki0427/deepseek-harness-desktop）
- 安装包产物输出到 dist 子目录，用户实际安装的位置是 G:\DeepSeek-Harness（与本目录无关）

---

## 二、根目录文件清单（每个文件干什么）

| 文件 | 作用 | 是否进安装包 |
|---|---|---|
| main.js | ★核心：Electron 主进程。窗口/托盘/服务启停/看门狗/崩溃自恢复/单实例/皮肤部署全在这里 | 是 |
| build.js | ★打包编排脚本：精简运行时 → 调 electron-builder → 注入资源 → 自动清理旧产物 | 否（仅打包时用） |
| preload.js | 渲染进程安全桥接，只暴露版本号和名称给页面 | 是 |
| index.html | 主题启动页（鲸鱼娘背景 + 徽章 + 加载动画），服务就绪前显示 | 是 |
| package.json | 项目配置：版本号、npm 脚本（start/build/release:*）、electron-builder 打包配置（唯一来源） | 是（版本字段） |
| package-lock.json | 依赖版本锁定，保证可重复构建 | 否（构建用） |
| .gitignore | git 排除规则：node_modules、dist、harness-runtime 运行时、github-package、备份等 | 是（git 用） |
| README.md | GitHub 项目主页文档（简介/特性/架构/下载/打包/皮肤来源标注） | 否（git 用） |
| LICENSE | MIT 开源许可 | 否（git 用） |
| icon.ico | 应用图标（多尺寸 256/128/64/48/32/16，来自 04 俏皮头像版，圆角化） | 是 |
| installer-icon.ico | 安装器/卸载器图标（来自 01 伸手可爱版，圆角化） | 否（打包时用） |
| tray.png | 托盘图标 32×32（来自 01 伸手眨眼版，圆角化） | 是 |
| theme-bg.png | 启动页背景图（03 鲸鱼抱枕版模糊+暗化） | 是 |
| theme-emblem.png | 启动页中央圆角徽章（03 鲸鱼抱枕版） | 是 |
| 项目说明书.md | 本文件 | 否（git 用） |

---

## 三、根目录文件夹清单（每个文件夹干什么）

| 文件夹 | 作用 | 能不能删 |
|---|---|---|
| .git | git 版本库数据（提交历史） | ❌ 删了就没版本管理了 |
| dist | ★构建产物归档：dist\版本号\Setup.exe。只保留最近两个版本（当前 2.0.7/2.0.8） | 按需删旧版本 |
| harness-runtime | ★内置运行时：node.exe + @deepseek-ai/dsh + watchdog.js + 内置鲸鱼娘皮肤。开发模式(npm start)和打包都从这里取 | ❌ 核心，勿删 |
| icon-design | 图标工程：源图、生成脚本（make-*.ps1）、旧图标备份、预览、outputs | ✅ 可整体删除（不影响运行，但改图标会用到） |
| github-package | 本地工具：双击 make-package.cmd 生成与 GitHub 一致的源码包和文件清单（本身不上传） | ✅ 可删（可重建） |
| node_modules | 开发依赖（electron、electron-builder），仅打包/开发用 | ✅ 可删后 npm install 重建 |
| .dsh-profile-backup | dsh 用户配置备份（装皮肤前拍的快照，用于回滚） | ✅ 可删（回滚备用） |

---

## 四、核心 JS 文件详解

### 4.1 main.js（Electron 主进程，53KB，最重要）

按区块顺序：

1. 基本配置：本地服务地址 127.0.0.1:3080；开发环境用 harness-runtime，打包环境用安装目录的 resources\harness；内置 node.exe 和 dsh 入口路径
2. checkHarness()：快速探测 3080 是否响应（状态码判断）
3. identifyService()：识别 3080 上到底是不是真 Harness（查页面里的 __DSH_BOOT__ 标记），防止误挂别的程序
4. validateHarnessRuntime()：启动前检查 node.exe / dsh / watchdog.js 是否齐全
5. killProcessTree()：用 taskkill /T /F 结束整个进程树
6. startHarness()：防重入启动流程——识别端口身份 → 拉起 watchdog.js（看门狗→dsh）→ 等 60 秒 → 二次身份确认。启动期间崩溃快速失败
7. stopHarness() / restartHarness()：停止/重启（只杀自己启动的服务，不误杀别人的）
8. 崩溃自动恢复：scheduleAutoRestart() 按 2s/4s/6s 退避重试 3 次，失败弹窗；成功后自动把窗口切回 UI
9. ensureSkinProfile()：★开盒即用皮肤——首次启动自动把内置鲸鱼娘皮肤注册进用户 dsh 配置（不需要 pnpm）
10. openHarness() / updateTray() / createTray()：窗口唤起与托盘菜单（打开/显示/启动/停止/重启/状态/退出）
11. createWindow()：创建窗口，先显示本地启动页(theme 背景)，服务就绪后切到 3080 的 Harness UI；固定窗口标题 DeepSeek Harness Desktop v版本号；导航/外链白名单
12. shutdown() / bootstrap()：退出清理与启动编排（托盘→窗口→皮肤→服务→UI）
13. 单实例锁：重复启动只弹窗提示并聚焦已有窗口；before-quit / window-all-closed 生命周期处理

### 4.2 build.js（打包编排，15KB）

1. prepareHarnessRuntime()：把 harness-runtime 复制成临时 harness-build，剔除 .map/.md/.ts/.mts，校验关键文件（node.exe/dsh/watchdog/皮肤）
2. build()：调 electron-builder，打包配置全部来自 package.json（唯一来源，避免双份配置冲突）
3. afterPack 钩子：把精简运行时注入 win-unpacked 的 resources\harness
4. cleanupOldUnpacked()：★构建成功后自动删除其他版本的 win-unpacked，只保留最新版
5. finally：无论成败都删除临时 harness-build

### 4.3 preload.js（454 字节）

contextBridge 安全桥接：只暴露 version（主进程注入）和 name 给页面，不开任何 Node 能力。

### 4.4 harness-runtime\watchdog.js（看门狗，2.8KB）

由主进程用内置 node.exe 启动，它再拉起 dsh。每 3 秒检查 Electron 主进程是否存活，
一旦发现主进程被强杀，立即 taskkill 整个 Harness 进程树，杜绝残留进程占用 3080。

---

## 五、图标资源关系（icon-design 工程）

| 源图 | 去向 | 用途 |
|---|---|---|
| 源图\04_俏皮头像版.png | icon.ico | 应用图标（桌面/开始菜单/任务栏） |
| 源图\01_伸手可爱版.png | installer-icon.ico | 安装器图标 |
| 源图\01_伸手眨眼版.png | tray.png | 托盘图标 |
| 源图\03_鲸鱼抱枕版.png | theme-bg.png + theme-emblem.png | 启动页主题背景+徽章 |

生成脚本：make-icons.ps1（四件套）、make-tray.ps1（矢量鲸鱼娘托盘）、make-tray2.ps1（插画托盘）。
圆角参数 0.22（越大越圆）。旧图标备份和矢量托盘在 旧图标备份\ 目录。

---

## 六、常用操作速查

| 想干什么 | 命令 |
|---|---|
| 开发模式启动 | npm start |
| 只打包（版本不变） | npm run build |
| 普通改动发版（2.0.8→2.0.9） | npm run release:patch |
| 较大变更发版（2.0.8→2.1.0） | npm run release:minor |
| 特大变动发版（2.0.8→3.0.0） | npm run release:major |
| 推到 GitHub | git push（需开 Clash TUN） |
| 上传安装包 | GitHub Releases 页面拖入 dist\版本号\Setup.exe |
| 查看 GitHub 上传清单 | 双击 github-package\make-package.cmd |
| 卸载皮肤插件 | dsh plugin --profile web remove @dsh-external/dsh-client-ui-skin-maid-atelier |
| 回滚 dsh 配置 | 把 .dsh-profile-backup\web\ 4 个文件复制回 C:\Users\Misaki\.dsh\profiles\web\ |

发版命令 = 自动升版本号 + 自动打包 + 输出到 dist\新版本\（互不覆盖），并自动清理旧版中间产物。

---

## 七、数据都存放在哪里（重装/卸载不受影响）

| 数据 | 位置 | 说明 |
|---|---|---|
| API Key | C:\Users\Misaki\.dsh\.credentials.yaml | 用户级，不在安装包里，分享 exe 不会泄露 |
| 会话记录 | C:\Users\Misaki\.dsh\sessions | 同上 |
| dsh 插件/皮肤配置 | C:\Users\Misaki\.dsh\profiles\web | 皮肤自动部署也写这里 |
| 浏览器缓存 | %APPDATA%\deepseek-harness-desktop | Chromium 数据 |
| 安装目录 | G:\DeepSeek-Harness | 覆盖安装即可升级 |

卸载/重装只会动安装目录，上面所有用户数据都保留。

---

## 八、版本历史速览

- 2.0.9：黑匣子日志（文件日志 + 失败弹窗显示最近日志 + 托盘打开日志目录）
- 2.0.8：内置鲸鱼娘皮肤，开盒即用；窗口标题带版本号
- 2.0.7：窗口标题固定 + 皮肤来源标注
- 2.0.6：托盘图标换插画版
- 2.0.5：矢量鲸鱼娘托盘图标
- 2.0.4：鲸鱼娘图标四件套 + 主题启动页
- 2.0.3：稳定性与安全大版本（看门狗/自恢复/端口校验/单实例）

## 九、故障排查（黑匣子日志，v2.0.9+）

应用自带文件日志：%APPDATA%\deepseek-harness-desktop\logs\desktop.log（2MB 自动轮转）。
启动失败/自动恢复失败的弹窗会直接显示最近日志；托盘菜单有「打开日志文件夹」。

两种典型报错含义：

1. "无法连接 Harness：http://127.0.0.1:3080" —— 服务 60 秒内没起来（启动阶段失败）。
   常见原因：dsh 启动崩溃（看日志堆栈）、3080 被占（占用方响应异常时也会走到这里）。
2. "Harness 服务多次启动失败……自动恢复 3 次后" —— 服务曾启动成功，之后被反复杀死。
   常见原因：安全软件杀掉未签名的 node.exe（把安装目录加入杀软白名单即可）。

排查流程：让用户发弹窗截图 + desktop.log 文件。

## 十、后续路线图（已确认方向）

1. 代码签名：目前未签名，SmartScreen 红色警告。方案：购买 OV/EV 证书（约 300~1500 元/年）
   后把 signExecutable 打开接入签名；或上架微软商店。免费方案不存在。
2. 自动更新：接入 electron-updater + GitHub Releases 发布源，实现"检查更新→下载→提示重启"，
   取代手动下载覆盖安装。属于较大变更，走 release:minor（2.1.0）。
3. 观察 2.0.9 黑匣子日志收集到的朋友机器故障，定位并修复根因。

---

最后更新：与 README.md 同步维护；改完代码记得顺手更新本文件。
