# 插件市场功能 —— 需求确认与可行性分析

> 阶段：确认需求 ✅ → 可行性分析（本文档）→ 开发 → 测试 → 实施 → 上线
> 参照对象：dsh 生态插件市场（用户提供的参考仓库 dsh-market/dsh-market 及其衍生桌面端 aYang998122/dsh-desktop）

---

## 一、需求确认结果（2026-08-19 与用户逐项确认）

| 决策点 | 结论 |
|---|---|
| 参照形态 | 官方市场仓库 dsh-market/dsh-market 的「插件市场」体验：列表浏览 + 一键安装 |
| 数据源 | **自建静态 JSON**（放本项目 GitHub 仓库 market/plugins.json，完全自主可控） |
| 入口 | **托盘菜单新增「插件市场」→ 独立窗口**（不动 3080 主页面） |
| 功能范围 | 浏览 + 搜索、一键安装、卸载、启用/禁用、更新、装完自动重启 Harness（全选） |
| 发布侧 | 暂不做（插件作者发布仍走 GitHub PR / 命令行） |

---

## 二、现状盘点（调研结论）

### 1. dsh CLI 插件机制（读官方源码确认，v0.1.0-rc.6）

- `dsh plugin --profile web <args...>` 本质是**把参数原样转发给 pnpm**，在
  `C:\Users\<用户>\.dsh\profiles\web` 目录执行（spawnSync pnpm，依赖 PATH）。
- pnpm 完成后自动「reconcile」：扫描 dependencies，凡包 manifest 声明
  `dsh.bundle.patch` 的自动加入 `dsh.profile.bundles`；卸载/失去声明的自动移除。
  **安装、卸载、更新后 bundles 都会自动同步** —— 这是可以复用的核心能力。
- 已装状态 = profile package.json 的 dependencies + node_modules 内各包 manifest。
- CLI **没有** 启用/禁用命令（= 手动增删 bundles 数组），也没有列表/市场命令。

### 2. 官方 3080 设置页现状

- `dsh-host-plugin-inventory` + `dsh-client-ui-settings-plugin-inventory`：
  **只读**展示已加载插件的 Cordis Loader 状态，无安装/卸载/市场能力。
- 结论：本功能是真正的增量，不与官方 UI 冲突。

### 3. 本桌面 App 现状

- 已有多窗口/托盘框架、`restartHarness()` 自动重启、鲸鱼娘主题资产、日志系统。
- 运行时内置 `dsh-service.exe`（改名 node）+ 完整 dsh 依赖树（含 dsh CLI 本体）。
- 沙箱/杀软经验：沿用 dsh-service.exe 执行进程，规避 node.exe 特征拦截。

---

## 三、方案设计

### 架构总览

```
托盘菜单「插件市场」
   │ 点击
   ▼
独立窗口 market.html（鲸鱼娘主题）
   │ marketBridge（contextBridge）
   ▼
主进程 market 模块（新增 market.js）
   ├─ 市场数据：market/plugins.json（内置 + 在线多源更新）
   ├─ 已装状态：读 profile package.json + node_modules manifests
   ├─ 安装/卸载/更新：dsh-service.exe 执行内置 pnpm.cjs（cwd=profile）
   ├─ 启用/禁用：读写 dsh.profile.bundles（无 BOM UTF-8）
   └─ 完成后：restartHarness()（已有）
```

### 1. 市场数据源（自建静态 JSON）

- 文件：本项目仓库根目录 `market/plugins.json`，随安装包内置一份（离线兜底）。
- 在线更新多源依次尝试（单源失败自动切换）：
  1. `https://cdn.jsdelivr.net/gh/Misaki0427/deepseek-harness-desktop@main/market/plugins.json`（CDN，国内可达性最好）
  2. `https://raw.githubusercontent.com/Misaki0427/deepseek-harness-desktop/main/market/plugins.json`（需代理环境）
  3. 内置副本（最终兜底）
- Schema：`{ "version": 1, "plugins": [ { "name", "description", "author", "homepage", "tags" } ] }`
  —— 与 dsh-market 生态一致：`name` 即 npm 包名，安装 = pnpm add <name>。

### 2. 安装/卸载/更新（不需要用户装 pnpm）

- **内置 pnpm**：将 pnpm 作为 harness-runtime 的 npm 依赖打包进
  `resources\harness\node_modules\pnpm`，用 dsh-service.exe 执行
  `pnpm\bin\pnpm.cjs add/remove/update <pkg>`，cwd = profile 目录。
  用户机器**无需预装 node/pnpm**（此前装插件必须依赖命令行 + pnpm，是最大痛点）。
- bundles 同步：照官方 reconcile 逻辑在 App 内实现（约 40 行，源码已通读）：
  依赖声明 `dsh.bundle` 即入 bundles；卸载即出；内置 bundles 永不触碰。
- 兜底链：内置 pnpm → PATH 上的 pnpm → PATH 上的 dsh CLI → 友好报错指引。
- 需要网络（npm registry），失败给出明确提示（如开 Clash）。

### 3. 启用/禁用

- 直接增删 profile package.json 的 `dsh.profile.bundles` 条目（无 BOM 写回）。
- 内置 bundle（dsh-base / dsh-web-app / 皮肤）不显示开关，防止搞坏系统。

### 4. 安全与稳定性（吸取历史教训）

- **包名白名单校验**：`^@?[a-z0-9][a-z0-9._-]*(\/[a-z0-9._-]+)?$`，杜绝注入。
- **安装前自动备份** profile package.json 到 `.dsh-profile-backup`（目录已在 .gitignore），
  失败自动回滚并弹窗说明。
- 操作**串行化**（同一时刻只跑一个 pnpm）；窗口显示实时进度/日志。
- dsh-img 的 exports 补丁修复逻辑保留：卸载重装后自动重新打补丁。

### 5. UI（市场窗口）

- 950×680 独立窗口，复用鲸鱼娘主题背景与样式资产。
- 布局：顶部搜索框 + 刷新/数据源状态；中部插件卡片列表（名称/描述/作者/标签/
  已装状态徽章）；卡片按钮随状态切换：安装 / 更新 / 禁用·启用 / 卸载。
- 内置插件（dsh-img 等 dependency）可卸载但带提示；内置 bundle 只读置灰。

### 6. 版本与发布

- 本功能为较大变更 → 版本 **2.2.0**（release:minor 语义）。
- 上线 = 打包 → Releases v2.2.0（含 market/plugins.json 上传）→ 老版本自动更新推送。

---

## 四、风险与对策

| 风险 | 对策 |
|---|---|
| 用户机器无 node/pnpm | 内置 pnpm + dsh-service.exe，零环境依赖 |
| npm registry / GitHub 不可达 | 市场 JSON 内置兜底 + 多源 CDN；安装失败明确报错引导开代理 |
| 装坏插件导致 Harness 起不来 | 操作前备份 + 失败回滚 + 现有启动自愈/失败弹窗兜底 |
| 安全软件拦截（联想电脑管家案例） | 沿用 dsh-service.exe（已避开 node.exe 特征），文档提示白名单 |
| 市场 JSON 恶意条目 | 只读白名单字段 + 包名正则校验 + 每步用户点击确认 |

---

## 五、测试计划

1. **静态**：node --check、JSON schema 校验、包名正则单测。
2. **无网场景**：断网启动 → 内置 JSON 展示、操作报错友好不崩溃。
3. **启停/卸载（无网络，最安全）**：对 dsh-img 测 禁用→启用→重启生效；
   再测 卸载→自动备份→重装→补丁自动恢复。
4. **安装（真实网络，一次性）**：安装一个已验证兼容 web profile 的小插件，
   验证 reconcile、重启后出现在官方「Plugins」清单。
5. **回归**：皮肤自动部署、托盘、日志分文件、自动更新不受影响。

## 六、实施步骤

1. harness-runtime 增加 pnpm 依赖并验证打包体积/精简流程不破坏 pnpm。
2. 主进程新增 market 模块 + preload marketBridge + market.html 窗口 + 托盘菜单项。
3. 编写 market/plugins.json 初始条目（dsh-img、皮肤等已验证插件）。
4. 本地测试（npm start 开发模式）→ 用户实测 →「请打包」2.2.0 → 上传 Releases。

## 七、工作量估计

主进程 market 模块约 300 行；market.html/js 约 400 行；preload/bulid.js 改动约 60 行；
市场 JSON 初始 5~8 个条目。预计打包体积增加约 25 MB（pnpm 全量）。
