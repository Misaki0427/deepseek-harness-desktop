# 依赖
node_modules/

# 构建产物（安装包请发布到 GitHub Releases）
dist/
harness-build/

# 内置运行时（体积大，按 README 步骤重建）
harness-runtime/node_modules/
harness-runtime/dsh-service.exe
harness-runtime/node.exe

# 本地工具（GitHub 上传包视图，本身不上传）
github-package/

# 本地配置备份（dsh 插件配置回滚用）
.dsh-profile-backup/

# 日志与临时文件
*.log
.DS_Store
Thumbs.db
